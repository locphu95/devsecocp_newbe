docker build -t devsecops-server_for_mac .
docker run -d \
  -p 2222:22 \        # SSH
  -p 3000:3000 \      # Gitea
  -p 8080:8080 \      # Jenkins
  -p 5000:5000 \      # Docker Registry
  -p 6443:6443 \      # K3s API
  --privileged \
  --name devsecops-loc-fuho \
  devsecops-server_for_mac





docker build -t devsecops-server .
docker run -d --privileged --name devsecops_container_locfuho \
  -p 22:22 -p 3000:3000 -p 8080:8080 -p 5050:5000 -p 6443:6443 \
  devsecops-server



các lệnh cần thực hiện sau khi run container
docker exec -it devsecops_container bash -> truy cập container vừa đc start

update các package
# Trivy
curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh

# Semgrep
pip install semgrep

# SonarScanner
for win
curl -L -o /opt/sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip \
  && unzip /opt/sonar-scanner.zip -d /opt/ \
  && ln -s /opt/sonar-scanner-*/bin/sonar-scanner /usr/local/bin/sonar-scanner
curl -L -o /opt/sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip \
  && unzip /opt/sonar-scanner.zip -d /opt/ \
  && ln -s /opt/sonar-scanner-*/bin/sonar-scanner /usr/local/bin/sonar-scanner

for mac
mkdir -p /opt/sonar-scanner
curl -L https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006.zip -o /opt/sonar-scanner.zip
unzip /opt/sonar-scanner.zip -d /opt/
ln -s /opt/sonar-scanner-*/bin/sonar-scanner /usr/local/bin/sonar-scanner
